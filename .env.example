# Discord Configuration
# ตั้งค่าการแจ้งเตือน Discord สำหรับ RAG PDF Bot

# เปิด/ปิดการใช้งาน Discord (true/false) - สำหรับส่งคำตอบจาก Gradio
DISCORD_ENABLED=true

# Discord Webhook URL (สร้างได้จาก Discord Server Settings -> Integrations -> Webhooks)
# ใช้สำหรับส่งคำตอบจาก Gradio ไปยัง Discord
DISCORD_WEBHOOK_URL=YOUR_WEBHOOK_URL_HERE

# Discord Bot Configuration - สำหรับรับคำถามจาก Discord
# เปิด/ปิด Discord Bot สำหรับรับคำถาม (true/false)
DISCORD_BOT_ENABLED=false

# Discord Bot Token (สร้างได้จาก https://discord.com/developers/applications)
# ใช้สำหรับรับคำถามจาก Discord โดยตรง
DISCORD_BOT_TOKEN=YOUR_BOT_TOKEN_HERE

# คำนำหน้าคำสั่งสำหรับ Bot
DISCORD_BOT_PREFIX=!ask

# โมเดลเริ่มต้นสำหรับ Discord Bot
DISCORD_DEFAULT_MODEL=gemma3:latest

# ตอบคำถามที่ไม่มี prefix หรือไม่ (true/false)
# true = ตอบทุกข้อความ, false = ตอบเฉพาะที่มี prefix/mention
DISCORD_RESPOND_NO_PREFIX=true

# คำแนะนำการตั้งค่า:
#
# ✅ สำหรับส่งคำตอบจาก Gradio ไปยัง Discord:
# 1. ไปที่ Server Settings -> Integrations -> Webhooks
# 2. สร้าง Webhook ใหม่
# 3. คัดลอก Webhook URL มาใส่ใน DISCORD_WEBHOOK_URL
# 4. ตั้งค่า DISCORD_ENABLED=true
#
# 🤖 สำหรับรับคำถามจาก Discord (Bot):
# 1. ไปที่ https://discord.com/developers/applications
# 2. สร้าง Application ใหม่ -> Bot -> Create Bot
# 3. ได้ Bot Token มาใส่ใน DISCORD_BOT_TOKEN
# 4. ไปที่ Bot -> OAuth2 -> URL Generator
# 5. เลือก scopes: bot
# 6. เลือก permissions: Read Messages/View Channels, Send Messages, Embed Links
# 7. คัดลอก URL และเชิญ Bot เข้า Server
# 8. ตั้งค่า DISCORD_BOT_ENABLED=true
# 9. ตั้งค่า DISCORD_BOT_TOKEN เป็น Token ที่ได้
#
# 📝 การใช้งาน Bot:
# Bot รองรับ 3 วิธีในการถามคำถาม:
#
# 1. ใช้ prefix: !ask คำถามของคุณ
#    ตัวอย่าง: !ask PDF นี้เกี่ยวกับอะไร
#
# 2. Mention Bot: @ชื่อBot คำถามของคุณ
#    ตัวอย่าง: @RAGPDFBot บทที่ 3 พูดถึงอะไร
#
# 3. ถามโดยตรง (ถ้า DISCORD_RESPOND_NO_PREFIX=true)
#    ตัวอย่าง: สรุปหัวข้อหลักของเอกสาร
#
# ⚙️ การตั้งค่า:
# - DISCORD_RESPOND_NO_PREFIX=true  -> ตอบทุกข้อความ (สะดวกแต่อาจรบกวน)
# - DISCORD_RESPOND_NO_PREFIX=false -> ตอบเฉพาะที่มี prefix/mention (เป็นส่วนตัว)